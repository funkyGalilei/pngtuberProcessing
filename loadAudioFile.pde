 //file = new SoundFile(this, "untitled.wav");
  //file.play();
  //file.loop();
  
  //   audioDuration = file.duration();
  //audioDuration = int(floor(audioDuration * rateOfFrames));
  //  print("aduio duration in frame count: " + audioDuration + "\n");
  
  //amp.input(file);
  
  
   // audio limiter just in case?? it works so no need for this for now
  //if (ampAnalyzer >= 1) {
  //  ampAnalyzer = .9;
  //} else if (ampAnalyzer <= 0){
  //      ampAnalyzer = 0.01;
  //}
